const pool = require("../config/db");

// TODO: implement once the quiz-related table(s) are finalized in database/schema.sql
// Covers table(s) from the ERD relevant to "quiz".

module.exports = {};
